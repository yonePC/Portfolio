<div class="container">
  <div class="row">
    <div class="col-7 mt-3 mx-auto">
      <h2>通知一覧</h2>

          <% notifications = @notifications.where.not(action_user_id: current_user.id) %>
          <% if notifications.exists? %>

              <%= render @notifications %>

          <% else %>
          通知はありません
          <% end %>

    </div>
  </div>
</div>

